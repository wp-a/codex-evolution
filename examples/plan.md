# 合成反冗余审核样例

目标：为个人 Codex 历史生成可解释的协作分析报告。
范围：本机 JSONL 导入、统计和报告，不做云端账号全量同步。
验收标准：固定样本可重算分子和分母，热力图可定位原文。

先建立微服务与事件总线，再做本地统计。
每一步修改都进行 SHA256 哈希比对。
每个阶段设置三个 Gate，并执行三轮审核。
为所有可能的输入增加多层兜底。
生产部署前必须获得用户明确批准。
对下载的发布制品验证 SHA256 完整性。
